syntax = "proto3";

package ping;

// Define la ruta de salida para Go
option go_package = "./proto";

service PingService {
  rpc Ping (PingRequest) returns (PingResponse);
}

message PingRequest {
  string message = 1;
}

message PingResponse {
  string message = 1;
}